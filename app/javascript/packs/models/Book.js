export default class {
  constructor(options) {
    // this.id             = _.get(options, 'id',             null)
    // this.title          = _.get(options, 'title',          null)
    // this.author         = _.get(options, 'author',         null)
    // this.publisher_name = _.get(options, 'publisher_name', null)
    // this.isbn           = _.get(options, 'isbn',           null)
    // this.image_url      = _.get(options, 'image_url',      null)
  }
}
