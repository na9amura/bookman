= debug User.all.inspect
= form_for @user, as: :auth, url: user_tokens_path do |f|
  = f.label :email
  = f.email_field :email

  = f.label :password
  = f.password_field :password

  = f.submit :login
